using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class isWorkerSelected : MonoBehaviour
{
    public int nbWorkerSelected;

    // Start is called before the first frame update
    void Start()
    {
        nbWorkerSelected = 0;
    }

}
